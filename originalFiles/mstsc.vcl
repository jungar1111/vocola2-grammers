include Unimacro.vch;
# Voice commands for mstsc (Terminal Server Client)

include utilities.vch;
include remoteControl.vch;

IP config = ipconfig;

IP config slash all = ipconfig /all;

crasy start menu = {Alt+Home};

terminal window = here;