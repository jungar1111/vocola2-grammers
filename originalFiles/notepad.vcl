include Unimacro.vch;
# Voice commands for NOTEPAD

include keys.vch;
include devedit.vch;

Test Simple Command = "It works!";



Save and Close = {Alt+f}s{Alt+f}x;
#Kill Word = HeardWord(Delete, Next, Word);
Ssh = HeardWord(ssh, Lowercase, that);